{
    "name": "ai-code-explainer-root",
    "version": "1.0.0",
    "description": "Root script to run AI Code Explainer",
    "scripts": {
        "install-all": "npm install && cd client && npm install && cd ../server && npm install",
        "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
        "start": "npm run dev"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}